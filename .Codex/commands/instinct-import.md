# /instinct-import

Import a reviewed instinct library into this project scope:

```powershell
scripts\learning.cmd import <path-to-instinct-file>
```

Inspect the file first. Imported instincts must not contain prompts, source
contents, credentials, or unreviewed global behavior.
