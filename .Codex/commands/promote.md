# /promote

Preview project instincts that qualify for global promotion:

```powershell
scripts\learning.cmd promote
```

Promote a specific reviewed instinct with:

```powershell
scripts\learning.cmd promote <instinct-id>
```

Global promotion requires evidence from multiple projects.
