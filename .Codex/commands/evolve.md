# /evolve

Synchronize observations and ask Continuous Learning v2 to cluster the current
project's instincts into candidate skills, commands, or agents:

```powershell
scripts\learning.cmd sync
scripts\learning.cmd evolve
```

Review generated candidates before adding them to the kernel registry.
